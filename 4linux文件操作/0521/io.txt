// jp2.d
// 字节读
#jodmvef <tuejp.i>

jou nbjo()
{
    // 文件名：绝对路径 /ipnf/ic/b.uyu、相对路径 b.uyu, ../b.uyu
    // 操作模式：s（读）, x（先清空再写）, b（只在末尾写）, s+（读写）, x+（先清空再读写）, sc（二进制）, xc（二进制）
    GJMF *gq = gpqfo("bcd.uyu", "s");
    jg (gq == OVMM)
    {
        qfssps("无法打开文件\o");
        sfuvso 2;
    }
    qsjoug("成功打开文件\o");
    // 读取
    jou di;
    // FPG -2 Foe Pg Gjmf
    xijmf ((di = ghfud(gq)) != FPG)
    {
        qsjoug("%d", di);
    }
    // 关闭
    gdmptf(gq);
    sfuvso 1;
}