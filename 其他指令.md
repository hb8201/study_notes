# 指令
- `sudo`   以管理员身份执行特定指令
- `sudo su` 直接切换到 root 身份

- `exit`   退出当前身份

- `cat`   查看文件内容
- `cat/etc/passwd`   查看所有系统用户的信息

- `head,tail`        显示文件的开头或结尾部分的内容

- `grep ***`       筛选*** 

- `ps`      查看进程
- `echo`      用于在屏幕上输出文本信息或变量值

- `fconfig `                    查看当前系统的网络接口状态、IP 地址、子网掩码

## 文件管理

- `man`手册               q退出

- `ls`  list 列表方式显示文件信息                   ls | grep ***    筛选***                 ls -l      显示详细信息
- `cd`  change dir 切换下一个目录
- `cd ..` 返回上一个目录
- `pwd` print work dir 打印工作目录，我在哪
- `mkdir`   新建文件夹
- `touch`   新建文件
- `rmdir`   删除空文件夹
- `rm`      删除文件，非空文件夹  -r
- `mv`      移动、重命名
- `cp`      拷贝、复制

## UNIX/Linux 目录结构

相对路径：没有起始位置，cd abc, cd ../abc, cd ~/abc
绝对路径：cd /var, cd /etc/mysql

目录结构
- `/`       根目录                                                             
- `/bin`    binary 程序                                               存放所有用户都能使用的基本命令（如 ls、cp）
- `/sbin`   系统管理程序                                               存放系统管理员专用管理命令的目录，通常只有root用户才能执行其中的命令
- `/boot`   启动、引导程序 bootloader                                   包含启动 Linux 所需的核心文件
- `/root`   根用户，管理员的用户目录
- `/home`   用户目录，/home/bob, /home/tom, /home/alice
- `/dev`    设备（硬件）投影     device                                  设备文件目录，硬件设备（如硬盘 /dev/sda、键盘）
- `/proc`   进程投影            process
- `/sys`    系统投影            system
- `/etc`    配置信息（注册表）                                           统配置文件中心，存放所有系统和应用程序的配置文件
- `/mnt`    mount 挂载设备                                             用于临时挂载外部文件系统，如USB设备、DVD或网络文件系统,手动挂载
- `/cdrom`  挂载                                                       专门用于挂载光盘驱动器等光存储设备
- `/media`  挂载                                                      自动挂载可移动介质，如U盘、SD卡、CD/DVD等
- `/var`    可变，日志、部署     variable
- `/tmp`    临时目录            temporary
- `/usr`    unix system resouce                                         用户可读取的应用程序和文件（头文件）
- `/opt`    可选，手动安装软件资源                                         第三方软件安装目录
- `/lib`    库目录：OpenCV、mySQL                                         系统运行所需的共享库文件
- `/lib64`

## 文件权限
类型 所有者   同组    其他人
d   rwx     rwx     rwx

文件类型
- `d`   目录/文件夹                           包含访问其他文件的信息，用cd命令进入
- `-`   普通文件                              文本、二进制文件、可执行文件
- `l`   link 快捷方式,链接文件
- `s`   套接字文件                            
- `c`   字符设备文件                          如串行设备、音频设备、键盘、鼠标
- `b`   块设备文件                            如硬盘、U盘等存储设备

权限
- `r`   read
- `w`   write
- `x`   execute
- `-`   没有权限

用户
- `u`   user    用户
- `g`   group   同组
- `o`   other   其他
- `a`   all     所有

命令
- `chmod`   修改权限
- `chown`   修改所有者
- `chgrp`   修正组

rw- r-- r--
110 100 100
6   4   4


## apt - 高级包管理工具
- apt update        更新软件清单，不安装
- apt upgrade       执行更新，安装
- apt install sl
- apt remove sl
- apt show sl
- apt search sl

## 解压压缩
### zip
- `zip a.zip a.txt`                  压缩单个文件:zip 压缩包名称.zip 目标文件
- `zip a.zip a.txt b.txt`            压缩多个文件：zip 压缩包名称.zip 文件1 文件2 文件3.. 
- `zip -r a.zip /home/suer `         压缩目录（包括目录内所有内容）：zip -r 压缩包名称.zip 目标目录       -r:递归压缩子目录

- `unzip a.zip`                      解压到当前目录
- `unzip a.zip -d /home/user`        解压到指定目录                                                -d:目标目录
- `unzip -1 a.zip`                   查看压缩包内容（不解压）
### rar
- `rar a a.rar a.txt`                压缩单个文件：rar a 压缩包名称.rar 目标文件                      a:添加文件到压缩包
- `rar a a.rar a.txt b.txt`          压缩多个文件：rar a 压缩包名称.rar 文件1 文件2...
- `rar a a.rar /home/user`           压缩目录：rar a -r 压缩包名称.rar 目标目录

- `unrar x a.rar`                    解压到当前目录                                                x:保持原目录结构解压
- `unrar x a.rar /home/suer`         解压到指定目录
- `unrar 1 a.rar`                    查看压缩包内容（不解压）
### tar
打包
    tar -cvf abc.tar a.txt b.txt c.txt

压缩,额外加 z
    tar -czf abc.tar a.txt b.txt c.txt

解压
    tar -xzf abc.tar.gz


## 服务管理
### ssh
ssh bob@***        ***IP地址         远程控制服务器          其他终端输入
### nginx
1. 网页目录  /var/www
vim ***.html     

基础服务
- openssh-server / sshd 远程终端
- nginx / httpd     开源服务器
- mysql-server / 数据库

服务（网络）命令
- service       `*`
### service
- `service ssh status`    显示状态
- `service ssh start`     启动服务
- `service ssh stop`      关闭服务
- `service ssh restart`   重启服务
- systemctl     `*`
- netstat -tpnl             网络连接与端口查看命令
- nmcli                     用于配置网络接口、管理连接
- ss
- ifconfig                   查看当前系统的网络接口状态、IP 地址、子网掩码
- ping / gping              网络连通性测试工具

## 编辑器

- nano
- vi/vim
