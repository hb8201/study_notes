# Linux 设备

## 字符设备

设备发送和接收数据以字符的形式进行

字符设备驱动程序实现open close loctl read write等系统调用函数

### 字符设备驱动程序
- 自动配置和初始化子程序
- 服务I/O请求的子程序
- 中断服务子程序

### 架构
- 头文件：linux内核源代码中的头文件
- 声明许可协议：MODULE_LICENSE("GPL);
- 注册与注销
    - 初始化函数    init: static int _init myinit(void)
    - 释放函数      exit: static void _exit myexit(void)
    - 在驱动程序中通过调用 module_init() 和 module_exit() 关联具体的初始化和释放函数
- 定义 struct file_operations结构体类型的变量，关联IO操作和驱动程序中对该操作发的具体定义
- 实现IO接口

```c
//Test_Char_Dev_0819.c
// 字符设备
#include <linux/module.h>
#include <linux/kernel.h>
#include <linux/fs.h>
#include <linux/init.h>
#include <linux/types.h>
#include <linux/uaccess.h>

// cat /proc/devices        查看当前已占用的设备号，选择没有被占用的
#define MAJOR_NUM 238

// 定义一个模拟设备信息的数据
static int dev_value = 99;  // 内核空间的数据

// 声明许可协议
MODULE_LICENSE("GPL");

// 定义具体的IO实现：读写
// read(fd, buf, len)
static ssize_t test_char_read (struct file *fp, char __user *buf, size_t len, loff_t *t)
{
    // 将内核空间中 dev_value 的值写入到 用户空间
    int length = copy_to_user(buf, &dev_value, 4);

    return sizeof(dev_value);
}

static ssize_t test_char_write (struct file *fp, const char __user *buf, size_t len, loff_t *t)
{
    int length = copy_from_user(&dev_value, buf, 4);
    return sizeof(dev_value);
}

// 定义一个设备的IO接口     设备和设备文件对应操作的关联
static struct file_operations myfops =
{
    .owner =  THIS_MODULE,
    .read = test_char_read,   // 系统调用read的时候，具体的write
    .write = test_char_write,
};

// 初始化函数
static int __init myinit(void)
{
    // 完成设备初始化：设备硬件初始化和数据初始化

    // 向操作系统注册
    if (register_chrdev(MAJOR_NUM, "Test_Char_Dev_0819", &myfops))
    {
        // 注册失败
        printk("<4>Register Failed\n");
        return -1;
    }
    else
    {
        // 注册成功
        printk("<4>Register Success\n");
        return 0;
    }
    return 0;
}

// 释放函数
static void __exit myexit(void)
{
    printk("<4>Test_Char_Dev_0819 exit");
    unregister_chrdev(MAJOR_NUM, "Test_Char_Dev_0819");
}

module_init(myinit);
module_exit(myexit);
```
```
obj-m := Test_Char_Dev_0819.o

KERNELDIR := /lib/modules/$(shell uname -r)/build
PWD := $(shell pwd)

default:
	$(MAKE) -C $(KERNELDIR) M=$(PWD) modules

clean:
	rm -rf *.o *.ko *.mod.c *.mod *.symvers *.order
```
```c
// a.c
// 测试程序
#include <stdio.h>
#include <sys/stat.h>
#include <sys/types.h>
#include <unistd.h>
#include <stdlib.h>
#include <fcntl.h>

int main()
{
    int fd;
    int n, len = 0;

    fd = open("/dev/Test_Char_Dev_0819", O_RDONLY);
    if (fd < 0)
    {
        perror("Open");
        exit(-1);
    }
    len = read(fd, &n, 4);
    printf("n = %d\n", n);
    return 0;
}
```
### 内核空间与用户空间数据的交互
- unsigned long copy_from_user(void *to, const void __user *from, unsigned long n);     用户到内核
- unsigned long copy_to_user(void __user *to, const void *from, unsigned long len);     内核到用户

### 流程
- make                                          编译驱动
- sudo insmod Test_Char_Dev_0819.ko             加载模块
- sudo dmesg                                    查看日志
- sudo mknod /dev/test_dev c 238 0              创建备用节点
- sudo chmod 666 /dev/test_dev                  修改权限
- printf "\x7b\x00\x00\x00" > /dev/test_dev     写入 123     十六进制 7b 00 00 00 -> 123
- cc a.c -o a                                   编译测试程序
- ./a                                           运行
- sudo rmmod Test_Char_Dev_0819                 卸载模块
- sudo rm /dev/test_dev                         删除设备节点（可选）

- cat /proc/devices                             查看当前已占用的设备号

## 块设备

设备发送和接收数据以整个数据缓冲区进行

## 网络设备

## USB设备

内核源代码：/usr/src/linux-hwe-7.0-7.0.0/drivers/usb/usb-skeleton.c

