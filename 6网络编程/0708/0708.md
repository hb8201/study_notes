# 云服务
公：122.51.21.94

sudo apt update
sudo apt upgrade

sudo apt install build-essentail
sudo apt install fish

# 端口问题
80
sudo apt install nginx

sudo netstat -tpnl  // tcp
sudo netstat -upnl  // udp

# 程序运行

启动但阻塞当前终端
./recv

启动后挂后台
./recv &

与终端分离
nouup ./recv &

